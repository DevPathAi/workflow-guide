{
  "name": "workflow-guide",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "prepare-docs": "node scripts/prepare-docs.mjs",
    "dev": "node scripts/prepare-docs.mjs && vitepress dev docs",
    "build": "node scripts/prepare-docs.mjs && vitepress build docs",
    "preview": "vitepress preview docs"
  },
  "devDependencies": {
    "vitepress": "^1.6.3"
  }
}
